// Example program
#include <iostream>
#include <string>
#include <math.h>
using namespace std;

int main()
{
   int x, a, res, b, c;
   cin>>x >>a >>b>>c;
   if (x<=0){
       res = pow(x, 3)+a/b;
   }
   if (x=0) {
       sin(a/b)+4*b*c;
   } 
   if (x>0) {
       sqrt(x)+a/b;
   }
   cout<<res;
   
}